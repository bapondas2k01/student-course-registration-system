
Setting environment for using XAMPP for Windows.
zubay@ZUBAYED_AMIN c:\xampp
# mysql -h localhost -u root -p
Enter password:
Welcome to the MariaDB monitor.  Commands end with ; or \g.
Your MariaDB connection id is 90
Server version: 10.4.32-MariaDB mariadb.org binary distribution

Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> create database student_course_registration_system;
Query OK, 1 row affected (0.002 sec)

MariaDB [(none)]> show databases;
+------------------------------------+
| Database                           |
+------------------------------------+
| flower_db                          |
| hospital_appointment_system        |
| information_schema                 |
| java_app                           |
| mysql                              |
| performance_schema                 |
| phpmyadmin                         |
| student_course_registration_system |
| student_management_system          |
| student_reg_system                 |
| test                               |
+------------------------------------+
11 rows in set (0.001 sec)

MariaDB [(none)]> use student_course_registration_system;
Database changed
MariaDB [student_course_registration_system]> CREATE TABLE users (
    ->     id INT,
    ->     fullname VARCHAR(100) NOT NULL,
    ->     username VARCHAR(50) NOT NULL UNIQUE,
    ->     contact VARCHAR(20),
    ->     password VARCHAR(100) NOT NULL,
    ->     role VARCHAR(20) ENUM('admin', 'student') DEFAULT 'student'
    -> );
Query OK, 0 rows affected (0.055 sec)

MariaDB [student_course_registration_system]> INSERT INTO users (fullname, username, contact, password, role)
    -> VALUES ('Bankai', 'admin', '01792952023', '1234', 'admin');
Query OK, 1 row affected (0.007 sec)

MariaDB [student_course_registration_system]>


CREATE TABLE students (
    id INT,
    name VARCHAR(100),
    dept VARCHAR(100),
    phone VARCHAR(20),
    email VARCHAR(100),
    PRIMARY KEY (id)
);


CREATE TABLE courses (
    course_id INT,
    course_name VARCHAR(100),
    instructor VARCHAR(100),
    department VARCHAR(100),
    credit DECIMAL(4,2),
    capacity DECIMAL(6,2),
    PRIMARY KEY (course_id)

);

CREATE TABLE enrollments (
    enrollment_id INT,
    student_id INT,
    course_id INT,
    enrollment_date DATE,
    status ENUM('Enrolled', 'Completed', 'Dropped') DEFAULT 'Enrolled',
    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (course_id) REFERENCES courses(course_id)
    PRIMARY KEY (enrollment_id)
);

CREATE TABLE reports (
    report_id INT,
    report_title VARCHAR(255) NOT NULL,
    report_date DATE,
    report_type ENUM('Enrollment Summary', 'Course Completion', 'Student Activity') NOT NULL,
    report_data TEXT NOT NULL,
    PRIMARY KEY (report_id)
);
